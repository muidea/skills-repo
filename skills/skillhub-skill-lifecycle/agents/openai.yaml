interface:
  display_name: "skill-hub Skill Lifecycle"
  short_description: "Manage local skill lifecycle with skill-hub"
  default_prompt: "Use $skillhub-skill-lifecycle whenever a task edits, refreshes, syncs, archives, or publishes skills under .agents/skills so the content changes also go through skill-hub status, validate, feedback, and optional push."
